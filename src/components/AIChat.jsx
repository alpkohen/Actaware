/**
 * ActAware site is static HTML (no React app). The live chat UI is in `/js/ai-chat.js`
 * and is loaded from `dashboard.html`. This file exists so the feature is easy to find in `src/`.
 */
export const AI_CHAT_IMPLEMENTATION = "/js/ai-chat.js";
